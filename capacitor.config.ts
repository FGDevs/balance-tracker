import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.fitraka.balancetracker',
  appName: 'Balance Tracker',
  webDir: 'www'
};

export default config;
